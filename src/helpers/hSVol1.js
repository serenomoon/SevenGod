export const importAll = (r) => { return r.keys().map(r);}

// export const hSVol1 = (saga) => { 
//     const images = importAll(require.context(`../assets/${saga}`, false, /.(png|jpe?g|svg)$/));
//     return images
// } 
